# Daily - 2026-07-20

## P0 de confianca, seguranca e testes

- Atividades ignoradas deixaram de contribuir para sessoes analiticas, extremos, recordes, pace, consistencia e minutos ativos.
- O calendario passou a persistir e consumir `localDate`, `startDateLocal` e `timezone`, mantendo o instante UTC para ordenacao e cursor.
- Recordes agora aceitam somente best efforts oficiais e usam `elapsedSec`.
- Pace do periodo passou a ser agregado por tempo confiavel dividido por distancia confiavel; limites sanitarios foram ampliados por modalidade.
- Consistencia passou a incluir semanas vazias; semana parcial nao dispara recomendacao prescritiva.
- A antiga carga heuristica foi substituida na interface por minutos ativos semanais.
- O detalhe de atividade aplica o mesmo escopo de plano antes de devolver dados ou aceitar alteracao.
- O bearer token do Strava saiu da sessao cliente; rotas server-side leem o armazenamento criptografado e migram documentos plaintext oportunisticamente.
- Criptografia OAuth passou a ser obrigatoria em producao e no contrato de ambiente.
- A rota de IA passou a validar tamanho/schema, limitar arrays/textos, aplicar quota por usuario e sanitizar erros do provedor.
- Politica, termos, setup e definicoes de KPI foram alinhados com Strava, Garmin, saude e Google Gemini. `LEGAL_VERSION` foi incrementada.
- Foi criada automacao de testes com Node 22 e CI sem `next build`.

## Validacao

- `npm run check`: aprovado.
- 19 testes automatizados apos inclusao da matriz de perfis.
- Gate completo repetido apos os ajustes finais: aprovado, com 19/19 testes e typecheck limpo.
- `npm run build`: nao executado por solicitacao do responsavel pelo projeto.

## Riscos e operacao

- A mudanca de `LEGAL_VERSION` exige novo aceite dos usuarios.
- A assinatura do webhook deve conter `STRAVA_WEBHOOK_POST_TOKEN` na callback URL ou enviar o header equivalente.
- `OAUTH_TOKEN_ENCRYPTION_KEY` precisa estar configurada antes do proximo deploy de producao.
- Caches analiticos antigos serao invalidados pelas novas versoes e reconstruidos sob demanda.
