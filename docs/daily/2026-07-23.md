# Daily - 2026-07-23

## Garmin health data

- Adicionado suporte ao CSV mensal de VO2 max exportado do Garmin (`VO2 Maximo.csv` / `VO2 Maximo`): parser identifica o cabecalho normalizado, converte meses em portugues para `YYYY-MM-01` e salva `{ date, monthLabel, vo2Max }`.
- Criada subcolecao `users/{stravaId}/vo2Max` para armazenar os pontos mensais sem misturar com sono e peso.
- `POST /api/admin/upload-health` agora aceita sono, peso e VO2 max no mesmo upload, retorna `vo2MaxSaved` e continua restrito a admin.
- `GET /api/health` passou a devolver `{ sleep, weight, vo2Max }`.
- O dashboard agora mostra um painel de VO2 max na secao de saude/desempenho quando houver dados no recorte ativo.

## Correcao de atualizacao de peso

- Identificado que o CSV de peso novo estava em formato compativel, mas o painel nao refazia a leitura de `/api/health` apos upload.
- Adicionado `healthRefreshKey` no `DashboardClient` e callback de upload no `AdminControlPanel`; ao salvar sono, peso ou VO2, o dashboard busca novamente os dados de saude sem exigir reload manual.

## Validacao

- `npm run typecheck`: aprovado.
- `npm run test:smoke`: aprovado.
- `npm run check:utf8`: aprovado.
- `npm run build`: nao executado.