# Diario 2026-06-27

## Contexto

- Objetivo do dia: concluir o deploy em producao com dominio proprio, validar o login via Strava e iniciar a evolucao do produto para leitura multiesporte.
- Escopo trabalhado: Vercel, DNS, Firebase, fluxo OAuth do Strava, sync de atividades e redesign do dashboard.

## Mudancas realizadas

- Ajustado `dash/lib/auth.ts` para solicitar o escopo `profile:read_all` no OAuth do Strava.
- Ajustado o parsing do nome do atleta para tolerar perfil incompleto no callback inicial.
- Trocado o provider customizado pelo provider oficial do Strava em `next-auth`.
- Corrigidos os endpoints de token do Strava para `api/v3/oauth/token` em `dash/lib/auth.ts` e `dash/app/api/strava/webhook/route.ts`.
- Corrigido `dash/app/page.tsx` para serializar `meta.lastSync` do Firestore antes de passar dados para o Client Component.
- Expandido o sync em `dash/app/api/strava/sync/route.ts` para persistir todas as atividades retornadas pelo Strava, mantendo a contagem de corridas validas apenas como metrica de produto.
- Evoluido o sync para modo incremental: depois da primeira carga, a API passa a pedir ao Strava apenas atividades posteriores a ultima data salva no Firestore.
- Ajustado `dash/lib/strava.ts` para aceitar consulta incremental por data e evitar `paceSec` invalido em esportes que nao fazem sentido no mesmo criterio de corrida.
- Removido o limite de 200 atividades na home para que os KPIs passem a refletir a base historica salva no Firestore.
- Refeito `dash/components/DashboardClient.tsx` com filtro multiesporte, filtro por ano, comparativo de 28 dias, leitura de carga semanal, indicio de deload, cards dinamicos, tabela mais rica e alternancia entre modo claro e escuro.
- Refeito `dash/app/login/page.tsx`, `dash/app/layout.tsx` e `dash/app/globals.css` para criar uma identidade visual mais forte, tipografia mais intencional e experiencia coerente entre login e dashboard.
- Corrigida a codificacao dos arquivos principais para UTF-8, evitando falha de build no Next.js.

## Decisoes tecnicas

- O app deve ser testado no dominio final `dash.corridapratodos.com.br`, porque `NEXTAUTH_URL` aponta para esse host.
- O DNS do subdominio `dash` deve ficar como `CNAME` para o alvo recomendado pela Vercel e com `DNS only` na Cloudflare.
- O produto passa a salvar o historico multiesporte inteiro, mas a corrida segue como filtro padrao e lente principal da experiencia.
- O tema visual deve suportar modo claro e modo escuro no cliente sem depender de backend.
- Para manter custo e latencia sob controle, a estrategia recomendada e seguir para agregados anuais e mensais server-side nas proximas iteracoes.

## Validacao

- Vercel com `Framework Preset = Next.js` e `Root Directory = dash`.
- Dominio `dash.corridapratodos.com.br` reconhecido como `Valid Configuration` na Vercel.
- Firebase com Firestore criado e regras publicadas para uso server-side only.
- Login via Strava concluido com sucesso no dominio final antes da iteracao visual.

## Pendencias

- Nao foi executado `npm run build` nesta rodada por instabilidade/travamento no ambiente do usuario.
- Reexecutar a sincronizacao para preencher o banco com atividades nao relacionadas a corrida que antes eram descartadas.
- Evoluir agregados anuais e mensais no Firestore para baratear leituras e acelerar a home.
- Registrar o webhook do Strava apos estabilizar o novo fluxo de dados.
- Rotacionar segredos expostos durante o setup.

## Proximos passos

- Testar o sync incremental em producao.
- Validar no navegador os blocos de comparativo 28 dias, carga semanal e sugestao de deload.
- Implementar agregados por `ano`, `mes` e `esporte` no Firestore.
- Fazer a home ler agregados em vez de todas as atividades cruas.
- Adicionar paginacao na tabela e melhores marcas por distancia.

## Riscos ou observacoes

- Ler toda a base historica no SSR melhora a fidelidade dos KPIs, mas deve ser tratado como etapa intermediaria, nao arquitetura final.
- O sync incremental reduz chamadas e writes desnecessarios, mas ainda depende de consultas brutas para a renderizacao da home enquanto os agregados nao existirem.

## Backlog imediato

- Ajustar o filtro de esporte para mostrar apenas esportes com atividades dentro do recorte ativo do usuario, especialmente respeitando o ano selecionado.
- Evoluir a tabela de atividades recentes com paginacao real e opcao de navegar por toda a base filtrada.
- Avaliar uma tela ou drawer de detalhe por atividade para exibir mais contexto sem poluir a home.
- Implementar bloco de recordes por distancia com foco inicial em 2026.
- Definir se os recordes por distancia serao aproximados pela atividade inteira ou corretos via `best_efforts` / detalhamento da API do Strava.

## Atualizacao 2 - governanca, privacidade e consentimento

- Implementada camada de controle de acesso em `dash/lib/access.ts` com verificacao de admin por papel salvo no Firestore ou por lista `ADMIN_STRAVA_IDS`, alem de versao de aceite legal e cooldown de full sync.
- Ajustado `dash/lib/auth.ts` para bootstrap de papel: o primeiro usuario da base passa a admin por padrao, e usuarios seguintes entram como `user` quando nao houver papel previo.
- Ajustado `dash/app/api/strava/sync/route.ts` para aceitar `mode=full`, restringir esse modo a admin, aplicar cooldown de 7 dias e registrar lock temporario para evitar sync concorrente.
- Criado `dash/components/LegalGate.tsx` e a rota `dash/app/api/legal/accept/route.ts` para exigir aceite de termos e privacidade antes do acesso ao dashboard.
- Criada `dash/app/api/account/route.ts` com exclusao da base do proprio atleta via `recursiveDelete` no Firestore.
- Criadas paginas publicas `dash/app/privacy/page.tsx` e `dash/app/terms/page.tsx` para dar suporte minimo de transparência e base legal no produto.
- Evoluido `dash/components/DashboardClient.tsx` para mostrar ferramentas de admin, botao de exclusao de dados, links de privacidade/termos e paginacao simples na tabela com `Ver mais atividades`.
- Ajustado o filtro de esportes para considerar apenas esportes existentes dentro do ano ativo, reduzindo ruido na interface.

## Observacoes novas

- O aceite legal agora e gate real de produto: o usuario autentica via Strava, mas nao acessa a home enquanto nao registrar o aceite.
- A exclusao apaga a arvore do usuario no Firestore, mas a revogacao do app no Strava continua sendo uma acao complementar do proprio atleta fora do CPT Dashboard.
- Nao rodei `npm run build` nesta rodada, seguindo sua orientacao anterior para evitar travamento no ambiente local.

## Atualizacao 3 - quality gate e seguranca automatizada

- Criados scripts automatizados em `dash/scripts/` para validar ambiente (`check-env.mjs`), encoding UTF-8 sem BOM (`check-utf8.mjs`), segredos hardcoded (`check-secrets.mjs`) e smoke checks do fluxo critico (`test-smoke.mjs`).
- Atualizado `dash/package.json` com os comandos `typecheck`, `check`, `check:prod`, `check:env`, `check:utf8`, `check:secrets` e `test:smoke`.
- Criado `dash/.env.example` documentando todas as variaveis necessarias para build e deploy com placeholders seguros.
- Criado workflow `.github/workflows/quality-gate.yml` para rodar `npm ci` e `npm run check:prod` automaticamente em push para `main` e em pull requests.
- Os smoke checks cobrem os pontos minimos de governanca: gate legal na home, restricao do `full sync`, sessao obrigatoria para exclusao e exposicao dos links de privacidade/termos.

## Validacao desta etapa

- `npm run check:utf8` OK
- `npm run check:secrets` OK
- `npm run test:smoke` OK
- `npx tsc --noEmit` OK
- `npm run build` nao foi executado nesta rodada por decisao operacional anterior do ambiente local do usuario.

## Atualizacao 4 - refatoracao para reduzir leitura no Firestore

- Criado `dash/lib/dashboard.ts` para centralizar serializacao, query por ano, conversao do Firestore para atividade do dashboard e resumo de sync.
- Criada a rota `dash/app/api/activities/route.ts` para carregar atividades do ano sob demanda, em vez de depender da colecao inteira na renderizacao da home.
- Refatorada `dash/app/page.tsx` para buscar apenas `meta/sync` e um recorte inicial do ano mais recente, reduzindo drasticamente reads no carregamento principal.
- Refatorado `dash/app/api/strava/sync/route.ts` para recalcular e persistir `availableYears`, `totalActivities`, `totalRuns`, `totalsByType` e `newestActivityAt` no `meta/sync`.
- Ajustado `dash/app/api/strava/webhook/route.ts` para salvar atividade multiesporte e manter `availableYears` atualizado mesmo fora do sync manual.
- Refeito `dash/components/DashboardClient.tsx` para trabalhar com ano carregado sob demanda, manter filtro por esporte apenas sobre o recorte ativo e abrir detalhe local da atividade em modal.
- Estendido `dash/app/globals.css` com estilos do modal de detalhe e do botao inline da tabela.

## Impacto esperado desta etapa

- A home deixa de ler automaticamente milhares de documentos a cada acesso.
- O uso tipico passa a concentrar leituras no ano atual, que e a principal unidade de analise para o atleta.
- O historico completo continua disponivel, mas apenas quando o usuario pede explicitamente esse recorte.

## Atualizacao 5 - planos e escopo gratuito no backend

- Implementado em `dash/lib/access.ts` o conceito de `role`, `plan` e `scope` com regra de produto server-side.
- O usuario configurado em `ADMIN_STRAVA_IDS` agora e tratado como `master`, com visao total e plano `pro` automatico.
- Usuarios comuns passam a cair no plano `free` por padrao, com escopo de dados limitado a `Run` e `Walk` no ano de `2026`.
- `dash/app/page.tsx` agora calcula o escopo do usuario antes de decidir quais anos e atividades podem ser carregados.
- `dash/app/api/activities/route.ts` normaliza o ano solicitado e reforca o escopo do plano no backend, evitando bypass via chamada direta da API.
- `dash/app/api/strava/sync/route.ts` passou a filtrar o payload do Strava antes de salvar, reduzindo custo e impedindo que usuarios free persistam historico fora do plano.
- `dash/app/api/strava/webhook/route.ts` tambem respeita o plano: atividades fora do escopo gratuito sao ignoradas para usuarios comuns.
- A estrutura ficou pronta para liberar futuramente `TrailRun`, `Hike` e `VirtualRun`, mas o produto inicial manteve apenas `Run` e `Walk` para ficar mais simples e barato.
- Smoke checks atualizados para vigiar o enforcement de plano no backend.

## Atualizacao 6 - plano pro nao-master e promocao por ID

- Ajustado `dash/lib/access.ts` para diferenciar melhor `master/admin` de `pro`: apenas `master/admin` tem visao total e ferramentas privilegiadas.
- Criado suporte a `PRO_STRAVA_IDS`, permitindo promover atletas pagos por lista de IDs sem transformalos em administradores.
- O plano `pro` nao-master agora recebe historico ampliado de corrida ao longo dos anos, com `Run`, `TrailRun`, `VirtualRun`, `Walk` e `Hike`.
- O plano `free` continua limitado a `Run` e `Walk` em `2026`, preservando o teto de custo do produto.
- Ajustado `dash/lib/auth.ts` para bootstrap automatico do plano com base em `PRO_STRAVA_IDS`, mantendo `master` separado de `pro`.
- Atualizado `dash/.env.example` para documentar `PRO_STRAVA_IDS` junto de `ADMIN_STRAVA_IDS`.

## Atualizacao 7 - alinhamento de KPIs do produto

- Mapeado o conjunto atual de KPIs do painel a partir de `dash/components/DashboardClient.tsx` e `dash/lib/dashboard.ts`.
- Registrado em `docs/product/kpi-alinhamento.md` um inventario do que o painel mede hoje, onde a leitura ainda mistura volume com desempenho e quais regras de produto precisam ficar explicitas.
- Consolidada a ideia de separar a home em cinco camadas de leitura: resumo executivo, volume/frequencia, desempenho, comparativos e saude da progressao.
- Formalizada a regra de que `corrida qualificada` e criterio do produto CPT, nao regra nativa do Strava.
- Definida como prioridade funcional futura a multisselecao de anos e esportes, mas somente depois de estabilizar a taxonomia dos KPIs.

## Atualizacao 8 - refatoracao da home por camadas de leitura

- Refatorado `dash/components/DashboardClient.tsx` para alinhar a home com a taxonomia de KPIs definida no documento de produto.
- Adicionado `DashboardMeta` tipado no client, reduzindo a fronteira `any` para os metadados principais da home.
- Reescritos o hero e os textos de apoio para deixar explicito o que e resumo executivo, volume, desempenho, comparativo, consistencia e dado bruto.
- Criado o componente visual `SectionLead` para separar as camadas de leitura e reduzir a sensacao de painel unico sem hierarquia.
- Reclassificados os paineis com `eyebrow` contextual (`Volume`, `Desempenho`, `Comparativo`, `Consistencia`, `Produto`, `Qualitativo`).
- Ajustados rótulos ambíguos no topo, incluindo `Sessoes ativas`, `volume no recorte ativo` e `Peso da corrida`.
- Estendido `dash/app/globals.css` com estilos para metodologia, cards de leitura e divisores de secao.

## Validacao desta etapa

- `npm run typecheck` OK.
- `npm run build` nao foi executado nesta etapa, seguindo a orientacao operacional do ambiente local.

## Atualizacao 9 - diagnostico do deploy que nao refletiu no dominio

- Confirmado que `dash/.vercel/project.json` esta vinculado ao projeto Vercel `dash` (`projectName: dash`), nao ao projeto `cpt-dashboard` que vinha sendo usado com o dominio principal.
- Evidencia objetiva: os deploys recentes via CLI geraram URLs como `dash-amber-alpha.vercel.app`, o que prova que a publicacao foi para outro projeto.
- Isso explica por que as mudancas locais passaram em `build`, foram publicadas via CLI, mas nao apareceram no dominio/projeto que o usuario estava validando.
- Conclusao: o problema nao estava no codigo da home e sim no alvo de deploy.

## Atualizacao 10 - correcao do ano inicial truncado e evolucao dos filtros

- Identificada a causa do 2026 incompleto na home: o SSR entregava apenas `limit(160)` do `initialYear` e o client tratava esse recorte parcial como se ja fosse o ano carregado por completo.
- Refatorado `dash/components/DashboardClient.tsx` para trabalhar com cache por ano (`yearActivities`) e marcar o ano inicial como parcial ate que o fetch completo da rota `/api/activities` termine.
- Corrigido o fluxo para que o primeiro ano selecionado tambem faca carga completa sob demanda, eliminando o sumico de janeiro e fevereiro em 2026.
- Implementada multisselecao de anos com agregacao do recorte ativo no client, preservando a consulta por ano no backend.
- Implementada multisselecao de esportes no client, incluindo combinacao de filtros e leitura multiesporte real no mesmo painel.
- Adicionada paginacao real na tabela de atividades com navegacao por pagina, em vez de crescimento infinito via `Ver mais`.
- Adicionado painel de `Melhores marcas do recorte`, aproximado pela melhor atividade inteira dentro de tolerancias de distancia-alvo.
- Estendido `dash/app/globals.css` com estilos para resumo de filtros, grade de recordes e nova navegacao da tabela.

## Validacao desta etapa

- `npm run typecheck` OK.
- `npm run build` nao foi executado nesta etapa, seguindo a orientacao operacional combinada para evitar travamento no ambiente local sem sua confirmacao.

## Atualizacao 11 - primeira quebra do arquivo gigante do dashboard

- Extraidos tipos compartilhados para `dash/components/dashboard/types.ts`.
- Extraidos helpers, constantes, formatadores e metadados de esporte para `dash/components/dashboard/helpers.ts`.
- Extraidos componentes visuais reutilizaveis (`MetricCard`, `CompareTile`, `Panel`, `SectionLead`, `InsightItem`, `DetailItem`) para `dash/components/dashboard/ui.tsx`.
- `dash/components/DashboardClient.tsx` passou a focar principalmente em estado, fetch, selecao e composicao do painel, em vez de acumular tipos, helpers e renderizadores basicos no mesmo arquivo.
- A refatoracao foi feita sem mudar a regra de negocio da rodada anterior.

## Validacao complementar

- `npm run typecheck` OK apos a extracao modular.

## Atualizacao 12 - auto-reparo dos anos e sync sensivel a ampliacao de escopo

- Confirmado no comportamento de producao um caso de metadado inconsistente: `meta.availableYears` podia ficar preso em `2026` mesmo com `totalActivities` alto e base historica ja presente no Firestore.
- Ajustado `dash/app/page.tsx` para detectar esse estado quando o usuario tem `fullAccess`, recalcular os anos reais a partir das atividades salvas e persistir a correcao em `meta` com `metadataRepairedAt`.
- Extraido `extractAvailableYears` em `dash/lib/dashboard.ts` para reutilizar a mesma regra de derivacao tanto no sync quanto no auto-reparo do SSR.
- Ajustado `dash/app/api/strava/sync/route.ts` para identificar ampliacao de escopo (`free -> pro`, `pro -> master/admin`, ou expansao de anos/tipos`) e forcar reconstrução completa em vez de seguir incremental preso no escopo antigo.
- O cooldown de full sync continua valendo quando o usuario pede full manualmente, mas a reconciliacao automatica por ampliacao de escopo nao fica bloqueada por esse cooldown.
- Reordenado o bloco de `Melhores marcas do recorte` para aparecer primeiro na camada de apoio, reduzindo a chance de o usuario nao encontrar os PRs no layout.

## Validacao desta etapa

- `npm run typecheck` OK.
- `npm run build` nao foi executado por mim, conforme combinacao operacional do projeto.
