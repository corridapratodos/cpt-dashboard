# Diario - 2026-07-08

## Contexto

- Hardening inicial de seguranca apos a auditoria consolidada.
- Foco em reduzir exposicao gratuita e melhorar o armazenamento dos tokens OAuth sem quebrar usuarios ja existentes.

## Mudancas

### 1. Camada de criptografia opcional e compativel para tokens OAuth

- **Arquivos:** `dash/lib/oauth-tokens.ts`, `dash/lib/auth.ts`, `dash/app/api/strava/webhook/route.ts`
- Foi criada uma camada dedicada para persistir tokens do Strava em formato criptografado com AES-256-GCM quando `OAUTH_TOKEN_ENCRYPTION_KEY` estiver configurada.
- A leitura continua compativel com documentos antigos em plain text, permitindo migracao gradual sem quebrar o webhook nem o refresh de sessao.
- Quando a chave de criptografia existe, novos logins e refreshes passam a gravar os tokens no envelope `oauthTokens` e limpam os campos plaintext.

### 2. Webhook passou a priorizar header sobre query string

- **Arquivos:** `dash/app/api/strava/webhook/route.ts`
- O token de autenticacao do POST agora usa primeiro `x-cpt-webhook-token` e so cai para `?token=` como compatibilidade.
- Isso reduz exposicao em logs e prepara a retirada futura do token na URL.

### 3. IDs reais removidos de arquivos publicos de configuracao

- **Arquivos:** `dash/.env.example`, `.github/workflows/quality-gate.yml`
- O `client_id` real do Strava e os IDs reais de admin/pro sairam do arquivo de exemplo.
- O workflow de CI tambem foi alinhado com placeholders neutros e passou a incluir variaveis de webhook POST e chave de criptografia.

### 4. Validacao de ambiente para a chave de criptografia

- **Arquivos:** `dash/scripts/check-env.mjs`
- O validador agora aceita `OAUTH_TOKEN_ENCRYPTION_KEY` em tres formatos seguros: base64 de 32 bytes, hex de 64 chars ou string de 32 chars.
- A variavel continua opcional para nao quebrar deploy existente, mas quando presente passa a ser validada.

### 5. Smokes alinhados com a nova camada de seguranca

- **Arquivos:** `dash/scripts/test-smoke.mjs`, `dash/tests/security-smoke.test.mjs`
- Os testes de fumaca agora verificam a existencia da camada `oauth-tokens`, o uso dela no auth e a prioridade do header no webhook.

### 6. Correcao defensiva no cursor do sync incremental

- **Arquivo:** `dash/app/api/strava/sync/route.ts`
- O cursor incremental deixou de tolerar atividades futuras ao decidir o `after` enviado ao Strava.
- Isso evita o caso em que uma atividade salva com data adiantada faz o sync pular a corrida real do dia atual.
- O overlap de 12h continua ativo no caso normal. Quando houver atividade futura salva, o sync faz uma recuperacao defensiva de 7 dias para nao pular a corrida real do periodo.


### 7. Falha de sync agora vira alerta bloqueante

- **Arquivos:** `dash/components/DashboardClient.tsx`, `dash/app/globals.css`
- Mensagens de erro de sincronizacao deixaram de ficar so no fluxo visual da pagina.
- Quando o sync falha, o painel agora abre um modal bloqueante com a causa retornada pela API.
- Sucessos continuam discretos; falhas criticas deixam de ser faceis de ignorar, especialmente no mobile.


### 8. Sync passou a distinguir causa tecnica util

- **Arquivo:** `dash/app/api/strava/sync/route.ts`
- O backend de sync deixou de devolver sempre a mesma mensagem generica de falha.
- Agora ele classifica cenarios comuns como token Strava expirado, rate limit do Strava, quota do Firebase e pendencia de indice do Firestore.
- O erro real tambem passou a ser registrado no log do servidor com `stravaId` e modo solicitado, acelerando diagnostico de producao.


### 9. Painel passou a lembrar o recorte ativo

- **Arquivo:** `dash/components/DashboardClient.tsx`
- O dashboard agora persiste por atleta o estado de anos, esportes, janela, mes, semana e modo de visualizacao.
- Ao recarregar a pagina, o painel restaura o ultimo recorte valido em vez de voltar sempre ao default.

### 10. Sync e backfill deixaram de depender do indice de `bestEfforts + date`

- **Arquivos:** `dash/app/api/strava/sync/route.ts`, `dash/app/api/admin/backfill-efforts/route.ts`
- As rotas deixaram de usar a consulta com `where(bestEfforts == []) + orderBy(date desc)`, que era a principal candidata a exigir indice composto no Firestore.
- O filtro de atividades sem best efforts passou a ser feito em memoria sobre um lote recente ordenado por data.
- Isso reduz friccao operacional e elimina uma falha de sync que nao agregava valor de produto.


### 11. Modal de atividade passou a abrir acima do header e carregar parciais sob demanda

- **Arquivos:** `dash/components/DashboardClient.tsx`, `dash/app/globals.css`, `dash/app/api/activities/[stravaId]/route.ts`, `dash/lib/strava.ts`
- O modal de detalhe teve o `z-index` elevado para nao ficar preso atras do header sticky no desktop.
- O detalhe da atividade ganhou consulta sob demanda das parciais metricas (`splits_metric`) direto no Strava ao abrir o modal.
- As parciais km a km nao passaram a inflar a base principal; elas sao carregadas apenas quando o usuario pede o detalhe.

### 12. Parciais detalhadas viraram recurso de `master` e `pro`, com cache por atividade

- **Arquivos:** `dash/lib/access.ts`, `dash/app/api/activities/[stravaId]/route.ts`, `dash/components/DashboardClient.tsx`, `dash/scripts/test-smoke.mjs`
- O acesso ao km a km deixou de ficar aberto para qualquer conta autenticada.
- Agora o backend libera a leitura detalhada apenas para usuarios `master` ou com plano `pro`, incluindo admins.
- Quando uma atividade elegivel tem as parciais carregadas pela primeira vez, elas passam a ser gravadas no proprio documento da atividade com `splitsFetchedAt`.
- Nas aberturas seguintes do modal, o painel reaproveita a base e evita nova chamada ao Strava para a mesma atividade.
- O usuario `free` continua vendo o detalhe basico do treino, mas recebe mensagem clara de que o km a km e um recurso restrito.

### 13. Publicacao preparada para auto-deploy pelo projeto `cpt-dashboard`

- **Arquivos:** historico operacional do dia
- O lote final de hoje foi consolidado para subir pelo fluxo correto `git push -> Vercel auto-deploy` no projeto `cpt-dashboard`.
- A pasta paralela `plano-maratona-curitiba/` ficou explicitamente fora do commit para nao contaminar a publicacao do dashboard.
- Antes da publicacao, as validacoes leves permaneceram verdes em UTF-8, smoke e typecheck.

### 14. Inicio da quebra estrutural do `DashboardClient`

- **Arquivos:** `dash/components/DashboardClient.tsx`, `dash/components/dashboard/useDashboardSync.ts`, `dash/components/dashboard/useDashboardHistory.ts`, `dash/components/dashboard/AdminControlPanel.tsx`, `dash/components/dashboard/SyncStatusModal.tsx`, `dash/scripts/test-smoke.mjs`
- O fluxo de sync, backfill e feedback de falha saiu do componente principal e foi movido para `useDashboardSync` e `SyncStatusModal`.
- A tabela historica paginada deixou de carregar sua logica dentro do shell principal e passou para `useDashboardHistory`.
- O bloco de ferramentas administrativas foi extraido para `AdminControlPanel`, reduzindo acoplamento visual e operacional.
- O `DashboardClient.tsx` caiu de aproximadamente 1486 para 1133 linhas nesta etapa, preparando a proxima rodada de extracao sem mudar o comportamento do painel.
- Os smoke tests foram ajustados para validar a nova fronteira de modulos em vez de depender de implementacao inline no arquivo gigante.

### 15. Correcao da regressao que escondia anos antigos no dashboard

- **Arquivos:** `dash/app/page.tsx`, `dash/lib/dashboard.ts`, `dash/app/api/strava/sync/route.ts`, `dash/app/api/strava/webhook/route.ts`, `dash/scripts/test-smoke.mjs`, `dash/tests/security-smoke.test.mjs`
- Foi identificado que sync incremental e webhook estavam sobrescrevendo `meta.availableYears` apenas com os anos cujo cache era reconstruido naquela rodada.
- Isso fazia o painel perder anos antigos na lateral, mesmo com as atividades historicas ainda salvas no Firestore.
- A correcao passou a mesclar anos preservados do metadata com os anos reconstruidos na rodada atual, removendo apenas os anos explicitamente tocados pelo rebuild.
- A home tambem ganhou uma autorreparacao mais robusta para contas com acesso total: quando a faixa de anos fica suspeitamente curta e ainda nao foi validada, ela recompone `availableYears` a partir da base real.
- Foram adicionadas assercoes de smoke para evitar regressao futura nesse fluxo de anos visiveis.

### 16. Correcao do agregado mensal com atividades ignoradas

- **Arquivos:** `dash/lib/analytics-types.ts`, `dash/lib/activity-analytics.ts`, `dash/components/dashboard/analytics.ts`, `dash/app/page.tsx`
- Foi confirmado que o painel contava `excludedSessions`, mas ainda somava distancia e duracao das atividades ignoradas no agregado diario e mensal.
- O modelo analitico passou a guardar tambem `includedDistanceKm` e `includedDurationSec`, separados do bruto por esporte e por dia.
- As camadas do dashboard agora usam apenas os totais incluidos para volume, comparativos, carga e timeline de desempenho.
- O `ANALYTICS_CACHE_VERSION` foi elevado para invalidar agregados antigos e forcar o recarregamento correto sem carregar o erro historico para o ano inicial.

### 17. Extracao do header, filtros e estado vazio do `DashboardClient`

- **Arquivos:** `dash/components/DashboardClient.tsx`, `dash/components/dashboard/DashboardHeader.tsx`, `dash/components/dashboard/DashboardEmptyState.tsx`, `dash/scripts/test-smoke.mjs`
- O header sticky e toda a faixa de filtros deixaram de viver inline dentro do `DashboardClient`.
- O toggle mobile dos filtros tambem saiu do componente principal e passou a ficar encapsulado em `DashboardHeader`.
- O estado vazio inicial foi movido para `DashboardEmptyState`, limpando mais uma decisao de renderizacao grande do shell principal.
- Com essa etapa, o `DashboardClient.tsx` caiu de 1133 para 1016 linhas antes da proxima rodada de quebra dos blocos analiticos.
- Os smoke tests passaram a verificar os novos componentes para evitar retorno silencioso do header inline gigante.

### 18. Extracao do resumo, leitura analitica e interpretacao do `DashboardClient`

- **Arquivos:** `dash/components/DashboardClient.tsx`, `dash/components/dashboard/DashboardExecutiveSection.tsx`, `dash/components/dashboard/DashboardAnalysisSection.tsx`, `dash/components/dashboard/DashboardInterpretationSection.tsx`, `dash/components/dashboard/analytics.ts`, `dash/scripts/test-smoke.mjs`
- O miolo de leitura do painel saiu do shell principal e foi dividido em tres blocos semanticamente estaveis: resumo executivo, leitura analitica e interpretacao/comparativos.
- A tipagem passou a reaproveitar `DashboardSlices` exportado de `analytics.ts`, evitando recriar contratos paralelos e mantendo a quebra alinhada com a logica de agregacao existente.
- O `DashboardClient.tsx` caiu de 1152 para 956 linhas nesta rodada, sem mudar o comportamento visual esperado do dashboard.
- Os smoke tests passaram a cobrir explicitamente os tres componentes extraidos para evitar volta silenciosa do JSX inline.

### 19. Normalizacao de encoding sem BOM nos arquivos criticos

- **Arquivos:** `dash/app/api/strava/sync/route.ts`, `dash/app/api/strava/webhook/route.ts`, `dash/app/page.tsx`, `dash/components/dashboard/analytics.ts`, `dash/components/dashboard/DashboardEmptyState.tsx`, `dash/components/dashboard/DashboardHeader.tsx`, `dash/lib/activity-analytics.ts`, `dash/lib/analytics-types.ts`, `dash/lib/dashboard.ts`, `dash/tests/security-smoke.test.mjs`
- A checagem `check:utf8` ainda falhava por causa de arquivos com BOM, inclusive pontos centrais do sync, dashboard e analytics.
- Os arquivos foram regravados em UTF-8 sem BOM para estabilizar o pipeline de qualidade e evitar regressao de encoding em deploy futuro.

### 20. Extracao final de saude, historico e legal do `DashboardClient`

- **Arquivos:** `dash/components/DashboardClient.tsx`, `dash/components/dashboard/DashboardHealthSection.tsx`, `dash/components/dashboard/DashboardHistorySection.tsx`, `dash/components/dashboard/DashboardLegalSection.tsx`, `dash/scripts/test-smoke.mjs`, `dash/tests/security-smoke.test.mjs`
- As tres ultimas areas pesadas de JSX inline sairam do shell principal: graficos de saude, tabela paginada e controle legal da conta.
- O `DashboardClient` ficou concentrado em orquestracao de estado, sync, filtros, analytics e modais, sem carregar mais renderizacao longa de cada secao visual.
- O arquivo caiu de 956 para 702 linhas nesta rodada, consolidando a quebra estrutural pedida pela auditoria.
- Os smoke tests funcional e de seguranca foram atualizados para verificar os novos componentes em vez de depender de texto inline no shell principal.

### 21. Extracao da carga analitica e dos insights do shell principal

- **Arquivos:** `dash/components/DashboardClient.tsx`, `dash/components/dashboard/useYearAnalytics.ts`, `dash/components/dashboard/insights.ts`, `dash/scripts/test-smoke.mjs`, `dash/tests/security-smoke.test.mjs`
- O carregamento incremental dos agregados anuais saiu do `DashboardClient` e foi encapsulado em `useYearAnalytics`, incluindo cache em `sessionStorage`, leitura por lote e sinalizacao de erro local.
- A geracao dos insights automáticos e labels derivados de carregamento/ano foi movida para `insights.ts`, removendo um bloco grande de texto condicional do componente principal.
- O `DashboardClient` caiu de 705 para 495 linhas e ficou mais proximo de um shell de composicao, com menos `useEffect` e menos regra textual inline.
- Os smoke tests funcional e de seguranca foram ampliados para garantir a existencia da nova fronteira `useYearAnalytics` + `buildAnalysisInsights`.

### 22. Extracao do estado de visualizacao e da leitura de saude

- **Arquivos:** dash/components/DashboardClient.tsx, dash/components/dashboard/useDashboardViewState.ts, dash/components/dashboard/useHealthData.ts, dash/scripts/test-smoke.mjs, dash/tests/security-smoke.test.mjs`r
- Tema, recorte salvo, preview admin/atleta e toggles principais sairam do shell e passaram a viver em useDashboardViewState.
- A leitura de sono e peso saiu do componente principal e foi movida para useHealthData, isolando a chamada de /api/health.
- O DashboardClient ficou ainda mais proximo de um shell de composicao, com menos estado local e menos efeitos de infraestrutura misturados ao JSX.
- Os smoke tests foram atualizados para validar explicitamente os novos hooks e evitar regressao estrutural silenciosa.


### 23. Card de VDOT e zonas estimadas de corrida

- **Arquivos:** `dash/components/DashboardClient.tsx`, `dash/components/dashboard/analytics.ts`, `dash/components/dashboard/DashboardInterpretationSection.tsx`, `dash/components/dashboard/types.ts`, `dash/lib/dashboard-ai.ts`, `dash/app/globals.css`
- A leitura de apoio ganhou um card de VDOT estimado para corrida, calculado a partir dos candidatos de recorde ja presentes no cache analitico.
- A escolha prioriza best efforts oficiais recentes dos ultimos 90 dias, cai para marca recente estimada e, se necessario, usa a melhor marca historica do recorte carregado.
- O card exibe a formula usada como referencia e deriva zonas de pace estimadas para leve, maratona, limiar, intervalado e repeticao.
- O payload da leitura integrada passou a receber o bloco de VDOT quando existir, mantendo a regra de nao citar zonas quando o dado nao estiver disponivel.
- `npm run typecheck` e `npm run test:smoke` passaram apos a alteracao.
