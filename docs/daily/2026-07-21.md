# Daily - 2026-07-21

## Continuidade do plano pos-auditoria

- Validado que o lote P0 local ja cobre testes de dominio, data local, recordes oficiais, consistencia com semanas vazias, escopo free/pro/admin/master, IA com schema limitado e criptografia OAuth obrigatoria em producao.
- Corrigida a exposicao remanescente do bearer token do Strava na sessao do NextAuth: `session.accessToken` deixou de ser preenchido e o tipo publico `Session` nao declara mais esse campo.
- O token continua disponivel apenas em contexto server-side via JWT interno e armazenamento criptografado, com rotas usando `getServerStravaAccessToken` para sync, backfill e detalhe de atividade.

## Validacao

- `npm run test:domain`: aprovado.
- `npm run test:smoke`: aprovado.
- `node --test tests/security-smoke.test.mjs`: aprovado.
- `npm run typecheck`: aprovado.
- `npm run check:utf8`: aprovado.
- `npm run check`: aprovado, com 20/20 testes.
- `npm run build`: nao executado.

## Observacoes

- O pacote P0 de codigo esta consolidado no commit local `ea9b767`; scripts temporarios `finalize*.mjs`/`run-finalize*.mjs` e a pasta `plano-maratona-curitiba/` seguem fora do commit operacional.
## Best efforts historicos

- Confirmado que o acesso master continua com `allowedYears: all`; o problema da maratona de 2022 nao era bloqueio de escopo por ano.
- Identificado gargalo no backfill admin de best efforts: a rota olhava apenas a fila recente (`date desc`, limite 240), entao atividades antigas qualificadas podiam nunca ser enriquecidas.
- Ajustada a rota `POST /api/admin/backfill-efforts` para aceitar `?year=YYYY` e usar o cache anual como indice economico antes de buscar detalhes no Strava.
- O painel admin agora envia automaticamente o ano quando ha apenas um ano selecionado; selecionando `2022`, o botao passa a processar best efforts daquele ano sem varrer o historico completo.
